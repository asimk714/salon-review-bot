// Honest usage note
const StatsNote = () => (
  <p className="text-xs text-muted-foreground text-center -mt-2">
    Your drafts stay on this device. Nothing is stored or sent anywhere except
    to the AI service that drafts your review.
  </p>
);

export default StatsNote;
